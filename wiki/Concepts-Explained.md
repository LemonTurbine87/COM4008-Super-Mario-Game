# Concepts Explained

## Concepts I used
- **OOP:** (e.g. how tiles/enemies/Mario are modelled as classes and inheritance/interfaces used)
- **Arrays / data structures:** (e.g. 2D array for the tilemap)
- **Collision detection:** (e.g. bounding-box or Greenfoot's `getIntersectingObjects` / `getObjectsInRange`)
- **State machines:** (e.g. Mario's states — idle, running, jumping, falling)
- **Physics/movement:** (e.g. velocity, gravity, acceleration used for jump arcs)

## Demo script (draft)
1. Show the game running briefly.
2. Explain the requirement I have chosen and explain why it is non-trivial.
3. Walk through the key classes and methods that implement it.
4. Mention a bug I hit and how I've diagnosed and  fixed it.
5. Mention how I tested it.